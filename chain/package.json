{
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network hardhat",
    "full-deploy": "npm run compile && npm run test && npm run deploy",
    "format": "npx prettier --write --plugin=prettier-plugin-solidity 'contracts/**/*.sol'",
    "deploy_mumbai": "npx hardhat run scripts/deploy.ts --network mumbai"
  },
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4",
    "prettier": "^3.1.1",
    "prettier-plugin-solidity": "^1.3.1"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
